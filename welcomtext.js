const welcome = (number, groupname) => {
    return `𝐇𝐨𝐥𝐚 @${number}. Welcome to ${groupname}`
}
exports.welcome = welcome

const bye = (number) => {
    return `OH you left @${number}.I wont miss you.
anyway see ya👋`
}
exports.bye = bye
